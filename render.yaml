services:
  - type: web
    name: loan-predictor-backend
    env: python
    rootDir: loan-predictor/backend
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.15